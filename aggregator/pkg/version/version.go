package version

var (
	// Version represents the current build version.
	Version = "dev"
	// Commit is the git commit hash for the build.
	Commit = ""
	// BuildDate is the ISO8601 timestamp of the build.
	BuildDate = ""
)
